{
    "expression_definition": {
      "symbols": ["+", "-", "*", "/", "sin", "cos", "sqrt", "^2", "^3", "^4", "^5"],
      "num_variables": 3,
      "has_constants": true
    },
    "expression_set_generation": {
      // Parameters for creating a new training set
      "num_expressions": 15000,
      "max_tree_height": 20,
      // Path where the generated expression set will be saved. If empty string, the expression set won't be saved
      "expression_set_path": "./data/expression_sets/rate_training_3v_20_15k.json"
    },
    "training": {
      // Path to the training set.
      "expression_set_path": "./data/expression_sets/rate_training_3v_20_15k.json",
      "epochs": 100,
      "batch_size": 64,
      "verbose": true,
      "latent_size": 128,
      "seed": 18,
      // Path where model parameters will be saved. If empty string, the parameters won't be saved
      "param_path": "./params/test.pt"
      //"param_path": "./params/rate_training_3v_20_15k.pt"
    },
    "symbolic_regression": {
  
    }
  }